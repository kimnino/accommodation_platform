# Phase 1 피드백

> 아래에 피드백을 작성해주세요. Claude가 이 파일을 읽고 반영합니다.

## 피드백

### 1
#2 (RoomType 유연성) — RoomType을 enum으로 고정하면 숙소마다 다른 객실 유형 표현이 어렵습니다. 제안: enum은 제거하고, 객실 유형명을 String으로 받되, 관리자가 숙소유형별 "추천 객실유형" 목록을 관리하는 방식.
파트너가 추천 목록에서 선택하거나 직접 입력 가능. 별도 테이블 없이 문자열로 저장하면 row 수 부담 없음.
> 직접입력하는건 없애고, 관리자가 String으로 제공할 수 있게 하자. 이상한 명칭으로 올려버리면 문제가 될거 같은데

#6 (partnerId) — 맞습니다. partnerId는 업체번호 개념이고, 소유권 검증용으로만 쓴 건 아닙니다. 향후 판매자 정보 표시에도 활용됩니다. 현재 서비스에서 소유권 검증도 하고 있는데, 이건 Security(Phase 7)에서
@CurrentUser 기반으로 전환하면 더 깔끔해집니다.
> 확인

#9 (breakfastIncluded) — 같은 객실에 "조식 미포함" / "조식 포함" 옵션을 분리하기 위한 필드입니다. 예: 스탠다드룸 - 조식 미포함 (10만원) / 스탠다드룸 - 조식 포함 (12만원). 이 방식을 유지할까요?
> 그럼 오션뷰, 포레스트뷰 등 또 다른 케이스가 나올때 마다 컬럼과 필드를 증가하면 리스크 있지 않을까?

#10 (Admin 승인 구조) — OTA 플랫폼에서 일반적입니다 (여기어때, 야놀자, Booking.com 등). 최초 등록 시 PENDING → 관리자 승인 → ACTIVE. 수정 건에 대해서는 수정 요청 → 관리자 승인 후 반영 구조를 추가하면 안전합니다.
지금은 즉시 반영이지만, 향후 AccommodationModificationRequest 같은 모델로 확장 가능합니다. 일단 현재는 최초 등록 승인 구조만 유지하고, 수정 승인은 Phase 1 범위 밖으로 두겠습니다.
> 오케이 수정에 대한 요청도 승인하는 구조로